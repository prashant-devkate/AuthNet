@model IEnumerable<AuthNet.UI.Models.DTO.CustomerDto>
@{
    ViewData["Title"] = "Customers";
}

<!-- Custom CSS for Badges (adapted for Customers) -->
<style>
    .badge-order-status {
        background-color: #ebedef;
        color: #3a3b45;
        font-weight: 500;
    }
</style>

<!-- Page Heading -->
<div class="d-sm-flex align-items-center justify-content-between mb-4">
    <h1 class="h3 mb-0 text-gray-800"><i class="fas fa-users me-2"></i> Customers</h1>
    <a asp-controller="Customers" asp-action="Add" class="d-none d-sm-inline-block btn btn-primary btn-icon-split btn-sm shadow-sm">
        <span class="icon text-white-50">
            <i class="fas fa-plus-circle"></i>
        </span>
        <span class="text">Add Customer</span>
    </a>
</div>

<!-- Content Row -->
<div class="row">
    <div class="col-12">
        <div class="card shadow mb-4">
            <div class="card-header py-3">
                <h6 class="m-0 font-weight-bold text-primary">Customers List</h6>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-bordered table-hover" id="dataTable" width="100%" cellspacing="0">
                        <thead>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Address</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Address</th>
                                <th class="text-center">Actions</th>
                            </tr>
                        </tfoot>
                        <tbody>
                            @if (Model != null && Model.Any())
                            {
                                @foreach (var customer in Model)
                                {
                                    <tr>
                                        <td class="fw-semibold">@customer.Name</td>
                                        <td class="text-muted">@customer.Email</td>
                                        <td class="text-muted">@customer.Phone</td>
                                        <td class="text-muted">@customer.Address</td>
                                        <td class="text-center">
                                            <a asp-action="Edit" asp-route-id="@customer.CustomerId" class="btn btn-warning btn-icon-split btn-sm me-1">
                                                <span class="icon text-white-50">
                                                    <i class="fas fa-pencil-alt"></i>
                                                </span>
                                            </a>
                                            <form asp-action="Delete" asp-route-id="@customer.CustomerId"
                                                  method="post" class="d-inline"
                                                  onsubmit="return confirm('Delete this customer? Note: This operation deletes all the orders associated with this customer.');">
                                                <button class="btn btn-danger btn-icon-split btn-sm">
                                                    <span class="icon text-white-50">
                                                        <i class="fas fa-trash"></i>
                                                    </span>
                                                </button>
                                            </form>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="5" class="text-center text-muted py-4">
                                        <i class="fas fa-info-circle me-2"></i>No customers found.
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function() {
            $('#dataTable').DataTable({
                "paging": true,
                "searching": true,
                "ordering": true,
                "info": true,
                "columnDefs": [
                    { "orderable": false, "targets": 4 }
                ]
            });
        });
    </script>
}